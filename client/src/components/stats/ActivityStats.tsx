import styles from "../../css/stats/ActivityStats.module.scss";

type Props = {};

const ActivityStats = ({}: Props) => {
	return (
		<div className={styles.ActivityStats}>
			<h2>Activity Stats</h2>
		</div>
	);
};

export default ActivityStats;
