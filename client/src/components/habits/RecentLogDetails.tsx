import styles from "../../css/habits/RecentLogDetails.module.scss";
import { RecentHabitLog } from "../../features/habits/types";

type Props = {
	recentLog: RecentHabitLog;
};

const RecentLogDetails = ({ recentLog }: Props) => {
	return (
		<div className={styles.RecentLogDetails}>
			{/*  */}
			{/*  */}
		</div>
	);
};

export default RecentLogDetails;
