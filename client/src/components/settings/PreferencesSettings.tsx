import styles from "../../css/settings/PreferencesSettings.module.scss";

type Props = {};

const PreferencesSettings = ({}: Props) => {
	return (
		<div className={styles.PreferencesSettings}>
			<h3>This is the Preferences page</h3>
			{/*  */}
			{/*  */}
		</div>
	);
};

export default PreferencesSettings;
