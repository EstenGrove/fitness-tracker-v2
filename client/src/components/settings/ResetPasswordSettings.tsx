import styles from "../../css/settings/ResetPasswordSettings.module.scss";

type Props = {};

const ResetPasswordSettings = ({}: Props) => {
	return (
		<div className={styles.ResetPasswordSettings}>
			<h3>This is the ResetPassword page</h3>
			{/*  */}
			{/*  */}
		</div>
	);
};

export default ResetPasswordSettings;
