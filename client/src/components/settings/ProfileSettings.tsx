import styles from "../../css/settings/ProfileSettings.module.scss";

type Props = {};

const ProfileSettings = ({}: Props) => {
	return (
		<div className={styles.ProfileSettings}>
			<h3>This is the Profile page</h3>
			{/*  */}
			{/*  */}
		</div>
	);
};

export default ProfileSettings;
