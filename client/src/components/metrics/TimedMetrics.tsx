import styles from "../../css/metrics/TimedMetrics.module.scss";

const TimedMetrics = () => {
	return (
		<div className={styles.TimedMetrics}>
			<div className={styles.TimedMetrics_header}>
				<h2 className={styles.TimedMetrics_title}>Timed Metrics</h2>
			</div>
		</div>
	);
};

export default TimedMetrics;
